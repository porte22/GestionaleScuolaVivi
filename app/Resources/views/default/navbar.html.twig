<nav class="navbar navbar-inverse">
    <div class="container-fluid">
        <div class="navbar-header">
            <a class="navbar-brand" href="#">No Logo</a>
        </div>
        <ul class="nav navbar-nav">
            <li class="active"><a href="{{ url('lista-classi') }}">home</a></li>
            <li><a href="{{ url('lista-classi') }}">Classi</a></li>
            {% if is_granted('ROLE_ADMIN') %}
                <li><a href="{{ url('fos_user_registration_register') }}">Registra utente</a></li>
            {% endif %}
        </ul>
    </div>
</nav>